# Get an imput of two digit number in string data type
two_digit_number = input("Type a two digit number: ")

# Convert first digit into integer
first_digit=int(two_digit_number[0])
# Convert second digit into ingeger
second_digit=int(two_digit_number[1])

# Printing first digit plus second digit, this will only work if you convert he digit into integer
print(first_digit + second_digit)